/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['csip-image.blr1.digitaloceanspaces.com', 'www.mangalmay.org', "images.unsplash.com"],
  },
};

export default nextConfig;
